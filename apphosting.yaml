# Settings to manage and configure an app hosting backend.
# Hosting configuration reference.

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
